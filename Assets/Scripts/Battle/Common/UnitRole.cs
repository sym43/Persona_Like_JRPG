using System;
using System.Collections.Generic;

/// <summary>
/// 전투원의 역할.<br/>
/// MainCharacter(주인공), Companion(동료), Enemy(적).
/// </summary>
public enum UnitRole
{
    MainCharacter = 0,
    Companion = 1,
    Enemy = 2
}
